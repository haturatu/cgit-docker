services:
  cgit:
    build:
      context: .
    container_name: cgit-server
    ports:
      - "${CGIT_PORT:-8080}:80"
    volumes:
      - "${GIT_REPOS_PATH:-/home/git/repos}:/var/lib/git/repos:ro"
    restart: unless-stopped
